#include<stdio.h>
int main(){
    int x;
    printf("Enter the value of x :");
    scanf("%d",&x);
    int y;
    printf("Enter the valuer of y :");
    scanf("%d",&y);
    int sum;
    sum=x + y;
    printf("The sum of two numbers is:%d",sum);
    
    return 0;
}